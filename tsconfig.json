{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
     "types": ["jest", "react", "react-native"],
    "jsx": "react",
    "paths": {
      "@/*": [
        "./*"
      ]
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx"
  ]
}